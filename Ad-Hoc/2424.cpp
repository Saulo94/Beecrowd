#include <bits/stdc++.h>
using namespace std;

int main(){
  int x, y;
  cin >> x >> y;

  if((x >= 0) && (x <= 432) && (y >= 0) && (y <= 468)){
    printf("dentro\n");
  }
  else{
    printf("fora\n");
  }


  return 0;
}
